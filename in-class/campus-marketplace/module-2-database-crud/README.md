# Module 2: SQLite and CRUD

This is the same Marketplace API, now stored in SQLite with SQLModel. It adds list filters plus update and delete operations. Start it with `uvicorn app.main:app --reload`, then use `/docs` to exercise the full CRUD flow.
